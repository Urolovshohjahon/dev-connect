import React from 'react';
import './Spinner.css';

function Spinner() {
    return (
        <div>
            <div className="loader" style={{color:'blue'}}>Loading...</div>
        </div>
    )
}

export default Spinner
